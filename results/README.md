# results/

Machine-written JSON/CSV only. Never hand-edited — see
`plan/00-agent-protocol.md` Rule 1 ("never invent a number") and Rule 3
("every result is reproducible"). Every file here carries a `provenance`
block (git SHA, timestamp, seed, config, dataset revision) written by
`pdt.provenance.write_result`.
