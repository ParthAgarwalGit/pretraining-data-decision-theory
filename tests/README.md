# tests/

pytest. Mirrors the `src/pdt/` package structure. `tests/theory/` holds the
numerical certificate suite from Phase 2 that checks every claimed
theoretical inequality against simulated data.
