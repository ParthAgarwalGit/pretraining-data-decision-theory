# paper/

LaTeX source for the submission. Populated starting Phase 6
(`plan/07-phase6-paper.md`). Numbers are injected from `results/*.json` via
`tools/render_numbers.py` — never typed directly into `.tex` files.
