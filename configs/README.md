# configs/

YAML configs, one or more per experiment. No experiment hardcodes a
parameter that could plausibly change between runs — model sizes, tasks,
metrics, bootstrap sample counts, and compute ceilings all live here.
